// map() - to map a function to all objects

// var arr=[1,2,3,4,5]

// console.log(arr.map(num=>num**2));

// // [ 1, 4, 9, 16, 25 ]

// console.log(arr);
// // [ 1, 2, 3, 4, 5 ]

// convert to uppercase

// var names=["raJ", "Akhil", "arun"]

// console.log(names.map(name=>name.toUpperCase()));

// if number is lesser than 5, num-1. if number is greater than 5, num-1.
// var arr=[2,3,4,6,7,8];

// console.log(arr.map(num=>num<5?num-1:num+1));


// filter()

// print even numbers in array
// var arr=[10,11,12,13,14,16,17,19,20]

// console.log(arr.filter(num=>num%2==0));

// print names starting with r
// var names=["ram", "Rahul","Akhil", "arun"]

// console.log(names.filter(name=>name[0]=="r"|name[0]=="R"));

// reduce()

// var arr=[10,11,12,13,14,16,17,19,20]

// console.log(arr.reduce((num1,num2)=>num1+num2));


// sort()
// some()
// find()
